# Files to ignore when pushing a PyCharm Django project to github:

#files only needed by a PyCharm project and local-only django files
.idea/
local_settings.py

#the virtual environment name - use the name of your virtual environment if different from
# those shown below. You will have only one of these folders.
.venv/
venv/
env/
ENV/

# Django files and folders
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/
__pycache__/

# Ignore Django Migrations in Development if you are working on team
# Only for Development only
**/migrations/**
# !**/migrations
!**/migrations/__init__.py

# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini

# Linux
*~